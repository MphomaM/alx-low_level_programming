#include "main.h"
#include <stdio.h>
/**
 * _abs - compute the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of the integer.
 */
int _abs(int n)
{

if (n >= 0)
return (n);
else
return (-n);

}
